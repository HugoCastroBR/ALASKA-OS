export type mouseContextMenuOptionsProps = {
  disabled?: boolean
  className?: string
  title: string
  onClick?: () => void
}

export type MouseProps = {
  mousePath: string;
  mouseInDesktop: boolean;
}